// @ts-nocheck
'use client'
// Nav is now just a top bar — navigation is in Sidebar
// This component is kept for backwards compat but Sidebar handles all nav
export default function Nav({ active = '' }) {
  return null
}
